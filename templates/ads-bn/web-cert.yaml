{{ if eq .Values.envAlias "prod" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ads-bn-cert
spec:
  secretName: ads-bn-cert
  dnsNames:
    - {{ .Values.domainBn }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
{{- end -}}
