{{ if eq .Values.envAlias "dev" }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ads-api-secret
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcp-secret-store      # name of the SecretStore (or kind specified)
  target:
    name: ads-api-secret        # name of the k8s Secret to be created
  data:
  - secretKey: API_AUTHEN_PASSWORD
    remoteRef:
      key: ads-api-authen
      property: API_AUTHEN_PASSWORD
      version: latest

  - secretKey: API_AUTHEN_USER
    remoteRef:
      key: ads-api-authen
      property: API_AUTHEN_USER
      version: latest

  - secretKey: API_ENDPOINT
    remoteRef:
      key: ads-api-authen
      property: API_ENDPOINT
      version: latest

  - secretKey: HIVE_USER
    remoteRef:
      key: ads-api-authen
      property: HIVE_USER
      version: latest

  - secretKey: HIVE_PASSWORD
    remoteRef:
      key: ads-api-authen
      property: HIVE_PASSWORD
      version: latest

  - secretKey: GOOGLE_SHEET_KEY1
    remoteRef:
      key: ads-api-authen
      property: HIVE_PASSWORD # Does not matter
      version: latest
{{- end -}}
